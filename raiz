import math
print("digite el numero que desea calcular su raiz cuadrada")
n = int(input())
if n < 0:
    print("no se puede calcular la raiz cuadrada de un numero negativo")
else:
    raiz = math.sqrt(n)
    print("la raiz cuadrada de", n, "es", raiz)
